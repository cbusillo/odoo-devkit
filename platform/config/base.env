# Shared environment defaults for all stacks (checked in, safe defaults only).
# Put runtime-environment overrides in the control-plane runtime environments
# file pointed to by `ODOO_CONTROL_PLANE_ROOT`.

LOCAL_ADDONS_DIRS=
ODOO_WITHOUT_DEMO=true
ODOO_HTTP_INTERFACE=0.0.0.0
ODOO_PROXY_MODE=True
ODOO_LIST_DB=False
